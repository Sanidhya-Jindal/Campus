# These run inside a Linux container. CRLF line endings would break the shell
# shebang ("bad interpreter: bash\r") and nginx/supervisor config parsing, so
# pin them to LF regardless of the checkout platform.
*.sh            text eol=lf
docker/*.conf   text eol=lf
Dockerfile      text eol=lf
